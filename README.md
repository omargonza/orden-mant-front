# Frontend (React + Vite)

## Setup
```bash
cd frontend
npm i
npm run dev
```
Crea un archivo `.env` con:
```
VITE_BACKEND_URL=http://localhost:8000
```
Abri `http://localhost:5173`, completá el formulario y presioná **Generar PDF**.
